<%= button_to "Edit Album", edit_album_url(@album), method: :get %>
<%= button_to "Delete Album", album_url(@album), method: :delete %>
<%= button_to "Up One Level", band_url(@album.band_id), method: :get %>

<br/>

<%= button_to "Create Track", new_album_track_url(@album), method: :get %>

<br/>

<% @album.tracks.each do |track| %>
  <%= link_to track.song, track_url(track), method: :get %>
  <br/>
<% end %>
