<% action_url = (note.comment.nil?) ? track_notes_url(track_id) : note_url(note) %>
<% action = (note.comment.nil?) ? "post" : "patch" %>

<form action="<%= action_url %>" method="post">
  <input type="hidden" name="_method" value="<%= action %>">
  <input type="hidden" name="authenticity_token" value="form_authenticity_token">

  <label>
    Comment:
    <br/>
    <textarea name="note[comment]" rows="8" cols="40"><%= note.comment %></textarea>
  </label>

  <br/>

  <input type="submit" value="Submit">
</form>
